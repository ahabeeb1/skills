---
bump: patch
why: example changeset — copy and edit, do not commit this file as a real changeset
---

# Example changeset

This file demonstrates the changeset schema. The release skill skips this file (reserved name).

To create a real changeset:

```bash
cp .changeset/EXAMPLE.md .changeset/<your-branch-slug>.md
```

Then edit the `bump:` and `why:` fields. Anything below this line is optional context.
