---
bump: minor
why: the grill now aligns user and agent across the whole workflow — it interrogates the slice decomposition (8th axis), verifies the user's expectations with tier-scaled mental-model probes, and gains a first-class re-grill edge so implementation-revealed spec ambiguity halts into a scoped round instead of being guessed away
---

# Grill 2.0 — full-workflow alignment

- `socratic-grill`: slice-shape 8th ambiguity axis (slice table is a standing Phase 1 inventory item); three mental-model probes (premortem, door classification with undo-cost follow-up, concrete example) scaled Quick 1 / Balanced 2 / Deep 3; scoped re-grill rounds with blast-radius resolution and dated back-linked `-regrill` records.
- `tdd-loop`: re-grill edge — `BLOCKED` + `suggested_action: "re-grill"`, 7-field learning payload, fixed-format halt block with two exits.
- `parallel-dev`: halt-scope rule (spec-wide pause-all default / slice-local run-to-completion, salvage of finished sibling results).
- `write-plan` / `decision-record`: consume the new "User mental model" grill-record section (success criteria → gate candidates; door labels → consequences).
- Dogfood 37/38/39 (executable assertions + LLM-behavior fixtures).
