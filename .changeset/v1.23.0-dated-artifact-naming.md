---
bump: minor
why: Decouple decision identity from releases — decision-record writes dated YYYY-MM-DD-<slug>.md ADRs at creation (slug is the uniqueness key, halt-loud on duplicate); the release-driven late-binding rename machinery is removed; convention extends to specs/plans/grill-records with version in frontmatter; the 24 integer ADRs stay frozen.
---

# v1.23.0 — Dated artifact naming

Supersedes the late-binding-ADR-ID half of `0020-late-binding-and-changesets` while retaining its Changesets-shape version-bump half (this very changeset). See the spec at `docs/agents/specs/v1.23.0-dated-artifact-naming.md` and the superseding dated ADR landed in Slice 6.
