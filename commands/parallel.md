---
description: Dispatch parallel subagents for independent work. Verifies independence before launch.
---

You are running the `parallel-dev` skill from habeebs-skill.

Read `${CLAUDE_PLUGIN_ROOT}/skills/parallel-dev/SKILL.md` and follow it exactly.

Batch to dispatch: $ARGUMENTS

Phase 2 (independence verification) is MANDATORY. If anything fails the independence check, do NOT dispatch in parallel — either restructure or run sequentially.
