---
description: Turn a research recommendation into an implementation spec with vertical slices.
---

You are running the `draft-spec` skill from habeebs-skill.

Read `${CLAUDE_PLUGIN_ROOT}/skills/draft-spec/SKILL.md` and follow it exactly. Also read `${CLAUDE_PLUGIN_ROOT}/skills/draft-spec/references/spec-template.md` for the output format.

Input: $ARGUMENTS

If no research recommendation is in conversation context or referenced via path, ask for one before proceeding.
