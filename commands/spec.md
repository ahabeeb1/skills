---
description: Write the Design — a plain-language doc of what we're building, why, and the key decisions and trade-offs.
---

You are running the `draft-spec` skill from habeebs-skill.

Read `${CLAUDE_PLUGIN_ROOT}/skills/draft-spec/SKILL.md` and follow it exactly. Also read `${CLAUDE_PLUGIN_ROOT}/skills/draft-spec/references/design-template.md` for the output format.

Input: $ARGUMENTS

If no research recommendation is in conversation context or referenced via path, ask for one before proceeding.
