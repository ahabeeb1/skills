# Audit-only directory. Single-writer (parallel-dev). No reads during chain execution.
# See: docs/agents/adrs/0004-parallel-subagent-dispatch-contract.md Part 2.
# Retention: records older than 30 days eligible for pruning via /sync (v1.8.0+).
