# v1.21.0 SKILL.md Body Cleanup — Full Audit (line-by-line, all 12 files)

**Audited on:** 2026-05-25
**Auditor:** Claude Opus 4.7 (hand audit, no script)
**Research base:** [docs/agents/research/2026-05-25-skill-md-body-shape.md](../research/2026-05-25-skill-md-body-shape.md)
**Spec:** [docs/agents/specs/v1.21.0-body-cleanup.md](v1.21.0-body-cleanup.md)

## How to read this audit

Every offending line in every affected SKILL.md, with proposed action:

- **REMOVE** — delete the cruft snippet; surrounding sentence keeps its meaning
- **RESTATE** — rewrite the line to preserve the rule but drop the inline citation/version/date
- **MOVE-TO-POSTMORTEM** — relocate dated incident narrative to `docs/agents/postmortems/<date>-<slug>.md`; restate surviving rule without the date
- **KEEP** — citation is genuinely load-bearing in a way the cleanup rule shouldn't touch (rare)

Each entry shows the **before** snippet, the **after** snippet, the **category** (ADR-cite / version-tag / dated-incident), and a one-line **fidelity note**.

## Summary table

| File | ADR cites | Version tags | Dated incidents | Proposed action counts |
|---|---|---|---|---|
| skills/using-habeebs-skill/SKILL.md | 7 | 1 | 0 | REMOVE 0, RESTATE 8, MOVE 0, KEEP 0 |
| skills/prior-art-research/SKILL.md | 9 | 0 | 1 | REMOVE 0, RESTATE 9, MOVE 1, KEEP 0 |
| skills/parallel-dev/SKILL.md | 7 | 1 | 0 | REMOVE 0, RESTATE 8, MOVE 0, KEEP 0 |
| skills/tdd-loop/SKILL.md | 6 | 2 | 0 | REMOVE 1, RESTATE 7, MOVE 0, KEEP 0 |
| skills/verify-output/SKILL.md | 7 | 0 | 0 | REMOVE 0, RESTATE 7, MOVE 0, KEEP 0 |
| skills/using-worktrees/SKILL.md | 0 | 1 | 0 | REMOVE 1, RESTATE 0, MOVE 0, KEEP 0 |
| skills/release/SKILL.md | 5 | 1 | 0 | REMOVE 1, RESTATE 5, MOVE 0, KEEP 0 |
| skills/write-plan/SKILL.md | 2 | 0 | 0 | REMOVE 0, RESTATE 2, MOVE 0, KEEP 0 |
| skills/setup-habeebs-skill/SKILL.md | 4 | 0 | 0 | REMOVE 0, RESTATE 4, MOVE 0, KEEP 0 |
| skills/devex-review/SKILL.md | 1 | 0 | 0 | REMOVE 0, RESTATE 1, MOVE 0, KEEP 0 |
| skills/deep-modules/SKILL.md | 2 | 0 | 0 | REMOVE 0, RESTATE 2, MOVE 0, KEEP 0 |
| **TOTAL (11 files — `cross-session-detect` not previously audited)** | **50** | **6** | **1** | **REMOVE 3, RESTATE 53, MOVE 1, KEEP 0** |

Note: original count of 12 files included `socratic-grill` (not audited; was deferred to v1.21.0 per PT-5). On re-grep, socratic-grill is not in the affected file list. Final count: **11 files, 57 hits total**.

---

## File 1: skills/using-habeebs-skill/SKILL.md (8 hits)

### Hit 1 — line 39
**Category:** ADR cite (clustered, multiple in one paragraph)
**Action:** RESTATE
**Before:**
```
This shape positively matches [OpenAI's Agents SDK](https://openai.github.io/openai-agents-python/multi_agent/) "Handoff = ownership transfer" primitive — handoffs at the markdown layer carry full state via the file the next skill reads, not via the pointer string. It also guards against the failure mode documented in Walden Yan's ["Don't Build Multi-Agents"](https://cognition.ai/blog/dont-build-multi-agents) (Cognition AI, 2025-06-12): subagents on degraded context (handoff strings without the parent's full trace) silently encode conflicting interpretations of the parent task. The full-doc-read contract is what keeps habeebs-skill's chain on the right side of Yan's line. Same invariant applies to `parallel-dev` subagent dispatches per [ADR-0004 § Part 3](../../docs/agents/adrs/0004-parallel-subagent-dispatch-contract.md): subagents receive the parent's full context (Phase 1 context, decomposition, steering, SYSTEM_CONTEXT preamble) as one coherent payload, not a thin task summary.
```
**After:**
```
The full-doc-read contract keeps the chain on the right side of the "don't build multi-agents" line: subagents on degraded context (handoff strings without the parent's full trace) silently encode conflicting interpretations of the parent task. The same invariant applies to `parallel-dev` subagent dispatches — subagents receive the parent's full context (Phase 1 context, decomposition, steering, SYSTEM_CONTEXT preamble) as one coherent payload, not a thin task summary.
```
**Fidelity:** Preserves the full-doc-read rule + extends to parallel-dev. Drops the OpenAI / Cognition / ADR-0004 citations (ADR-0004 already governs parallel-dev directly; the OpenAI + Cognition links live in the research archive that motivated the rule).

### Hit 2 — line 55
**Category:** Version tag
**Action:** REMOVE
**Before:**
```
## Auto-invocation scope (v1.19.0+ per ADR-0007 § C)
```
**After:**
```
## Auto-invocation scope
```
**Fidelity:** Same section name; version archaeology dropped.

### Hit 3 — line 63
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Why: with 18 skills auto-invocable and 135 SKILL.md files installed system-wide, the fuzzy-match pool diluted entry points that should have won. Demoting chain-internals restores entry-point precedence while preserving the slash-command muscle memory. See ADR-0007 § C for the rationale and the per-skill demotion criteria.
```
**After:**
```
Why: with 18 skills auto-invocable and 135 SKILL.md files installed system-wide, the fuzzy-match pool diluted entry points that should have won. Demoting chain-internals restores entry-point precedence while preserving the slash-command muscle memory.
```
**Fidelity:** Identical reasoning preserved; "See ADR-0007 § C" trailer dropped. ADR-0007 still governs from the index.

### Hit 4 — line 67
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per [ADR-0011](../../docs/agents/adrs/0011-error-analysis-cadence.md), the chain has two complementary quality loops: `verify-output` (static, pre-commit, KNOWN slop classes) and chain-postmortems (dynamic, post-incident, NEW failure categories). Postmortems are where error analysis happens on real chain runs — Hamel Husain + Shreya Shankar's "[error analysis before infrastructure](https://hamel.dev/blog/posts/evals-faq/)" thesis applied to a markdown-only chain.
```
**After:**
```
The chain has two complementary quality loops: `verify-output` (static, pre-commit, KNOWN slop classes) and chain-postmortems (dynamic, post-incident, NEW failure categories). Postmortems are where error analysis happens on real chain runs.
```
**Fidelity:** Two-loop rule preserved. Drops the ADR-0011 cite + Hamel/Shreya attribution (lives in postmortems README + ADR-0011 itself).

### Hit 5 — line 76
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Artifact:** one markdown file at `docs/agents/postmortems/YYYY-MM-DD-<slug>.md` per incident. Template structure (transition-failure-matrix per Hamel + Shreya): see [`docs/agents/postmortems/README.md`](../../docs/agents/postmortems/README.md).
```
**After:**
```
**Artifact:** one markdown file at `docs/agents/postmortems/YYYY-MM-DD-<slug>.md` per incident. Template structure: see [`docs/agents/postmortems/README.md`](../../docs/agents/postmortems/README.md).
```
**Fidelity:** Forward-pointer to postmortems README preserved (this is Pattern D: forward-only footer-style link, allowed). Drops "transition-failure-matrix per Hamel + Shreya" attribution.

### Hit 6 — line 80
**Category:** ADR cite + version tag combined
**Action:** RESTATE
**Before:**
```
**v1.11.0 promotion criterion** (per ADR-0011): if 10+ real postmortems land in 90 days OR the user explicitly requests a `/postmortem` slash-command → promote this section to a standalone `chain-postmortem` skill with description tuned to failure-shaped trigger phrases.
```
**After:**
```
**Promotion criterion:** if 10+ real postmortems land in 90 days OR the user explicitly requests a `/postmortem` slash-command, promote this section to a standalone `chain-postmortem` skill with description tuned to failure-shaped trigger phrases.
```
**Fidelity:** Trigger condition preserved verbatim. Drops "v1.11.0" version tag and "per ADR-0011" cite.

### Hit 7 — line 84
**Category:** ADR cite (multiple in one sentence)
**Action:** RESTATE
**Before:**
```
A separate curated ledger was evaluated and rejected (see ADR-0014 grill, 2026-05-18): it duplicates these artifacts, decays without the automated pruning ADR-0002 forbids, and reverses ADR-0010's doc-weight reduction. If you want a "learnings file", write an ADR or a postmortem instead.
```
**After:**
```
A separate curated ledger was evaluated and rejected: it duplicates these artifacts, decays without the automated pruning the standalone-by-design constraint forbids, and reverses prior doc-weight reductions. If you want a "learnings file", write an ADR or a postmortem instead.
```
**Fidelity:** Three rejection reasons preserved. ADR-0014 grill reference + date dropped (grill record is searchable). "ADR-0002 forbids" restated as "standalone-by-design constraint forbids" — same rule, neutral language. ADR-0010 reference removed; effect preserved.

### Hit 8 — line 111
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
See [`docs/agents/adrs/0002-habeebs-skill-standalone.md`](../../docs/agents/adrs/0002-habeebs-skill-standalone.md).
```
**After:**
```
(Section deleted — the "Standalone by design" section header that precedes this line already names the constraint; the trailing cross-link is the forbidden backward citation.)
```
**Fidelity:** The section heading "## Standalone by design (ADR-0002)" itself is Hit 9 below — fold both together: rename heading to "## Standalone by design" and delete the trailing "See ADR-0002" link. Rule preserved in section body.

### Hit 9 — line 105
**Category:** ADR cite (section heading)
**Action:** RESTATE (combined with Hit 8)
**Before:**
```
## Standalone by design (ADR-0002)
```
**After:**
```
## Standalone by design
```
**Fidelity:** Identical section; parenthetical ADR tag removed.

### Hit 10 — line 123
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
- **Research surfaces a blocker** — e.g., the chosen architecture violates [ADR-0002](../../docs/agents/adrs/0002-habeebs-skill-standalone.md) and the alternatives all violate it too
```
**After:**
```
- **Research surfaces a blocker** — e.g., the chosen architecture violates the standalone-by-design constraint and the alternatives all violate it too
```
**Fidelity:** Trigger preserved. "ADR-0002" replaced with its plain-English referent.

### Hit 11 — line 154
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per [ADR-0012](../../docs/agents/adrs/0012-compress-at-overflow-protocol.md), the chain's 4th context-engineering move ("Compress-at-overflow") is a markdown-only summary-and-flush protocol for sessions that approach context-window pressure. The other three moves (Write, Select, Isolate per [LangChain's framework](https://blog.langchain.com/context-engineering-for-agents/)) are already covered by SYSTEM_CONTEXT writes, prior-art-research fetches, parallel-dev dispatch isolation. This section closes the Compress gap.
```
**After:**
```
The chain's 4th context-engineering move ("Compress-at-overflow") is a markdown-only summary-and-flush protocol for sessions that approach context-window pressure. The other three moves (Write, Select, Isolate) are already covered by SYSTEM_CONTEXT writes, prior-art-research fetches, and parallel-dev dispatch isolation. This section closes the Compress gap.
```
**Fidelity:** Full rule preserved including the 4-of-4 enumeration. Drops ADR-0012 cite and LangChain framework attribution.

### Hit 12 — line 175
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
`.scratch/` is gitignored by user convention (not enforced by ADR — these are ephemeral working-set files, not durable artifacts). Cleanup is manual or per-user-environment policy. Per ADR-0002, no runtime substrate manages the lifecycle.
```
**After:**
```
`.scratch/` is gitignored by convention — these are ephemeral working-set files, not durable artifacts. Cleanup is manual or per-user-environment policy. No runtime substrate manages the lifecycle.
```
**Fidelity:** All three facts preserved (gitignored, cleanup manual, no runtime substrate). Both ADR cites dropped.

### Hit 13 — line 177
**Category:** ADR cite + version tag combined
**Action:** RESTATE
**Before:**
```
**v1.11.0 promotion criterion** (per ADR-0012): 3+ postmortems in `docs/agents/postmortems/` showing Context Distraction OR Context Confusion as the failure mode → promote this passive doc to an active skill (`chain-overflow-flush` or similar) with richer detection heuristics.
```
**After:**
```
**Promotion criterion:** 3+ postmortems in `docs/agents/postmortems/` showing Context Distraction OR Context Confusion as the failure mode → promote this passive doc to an active skill (`chain-overflow-flush` or similar) with richer detection heuristics.
```
**Fidelity:** Same as Hit 6 pattern — promotion trigger preserved; "v1.11.0" + ADR cite dropped.

### Hit 14 — line 196
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Chain skills consume `docs/agents/SYSTEM_CONTEXT.md` as their environment-binding cache. The canonical freshness/staleness protocol is documented once and consumed by all 10 chain skills that read the file: [`docs/agents/references/system-context-staleness-check.md`](../../docs/agents/references/system-context-staleness-check.md). Per ADR-0005, only `prior-art-research` Phase 0 writes SYSTEM_CONTEXT.md (single-writer invariant); all other skills only read.
```
**After:**
```
Chain skills consume `docs/agents/SYSTEM_CONTEXT.md` as their environment-binding cache. The canonical freshness/staleness protocol is documented once and consumed by every chain skill that reads the file: [`docs/agents/references/system-context-staleness-check.md`](../../docs/agents/references/system-context-staleness-check.md). Only `prior-art-research` Phase 0 writes SYSTEM_CONTEXT.md; all other skills only read.
```
**Fidelity:** Single-writer rule preserved as a direct statement. "Per ADR-0005" dropped. Forward-link to staleness-check reference kept (Pattern D allows forward links). "10 chain skills" hardcoded count replaced with "every chain skill that reads" — same rule, no count drift.

---

**File 1 total: 14 hits → final action counts: REMOVE 1, RESTATE 13, MOVE 0, KEEP 0.** (Original estimate said 8; on re-pass found 14. The increase is from re-classifying clustered ADR cites as separate hits rather than one paragraph-level hit.)

---

## File 2: skills/prior-art-research/SKILL.md (10 hits)

### Hit 1 — line 41
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
- **File stale** → emit the staleness banner and refresh inline (prior-art-research is the canonical SYSTEM_CONTEXT.md writer per ADR-0005 single-writer invariant; other chain skills only read).
```
**After:**
```
- **File stale** → emit the staleness banner and refresh inline (prior-art-research is the canonical SYSTEM_CONTEXT.md writer; other chain skills only read).
```
**Fidelity:** Single-writer rule preserved. ADR-0005 reference dropped.

### Hit 2 — line 56
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
This gate is adaptive, never a hard block (see ADR-0013, generalized chain-wide by ADR-0016).
```
**After:**
```
This gate is adaptive, never a hard block.
```
**Fidelity:** Rule (adaptive, not blocking) preserved. Both ADR cites dropped — they document the genealogy of the rule, not the rule itself.

### Hit 3 — line 118 (DATED INCIDENT)
**Category:** Dated incident
**Action:** MOVE-TO-POSTMORTEM
**Before:**
```
**Why this exists:** a single-agent Phase 2 planner reliably misses entire categories of architectural concern. The chain's bleeding pain (documented 2026-05-12) was a research run that missed `hooks / event handlers` and `subagent-driven patterns` for habeebs-skill itself — and the chain blindly proceeded against the incomplete decomposition. Phase 2.5 is the coverage gate that catches this.
```
**After:**
```
**Why this exists:** a single-agent Phase 2 planner reliably misses entire categories of architectural concern. Without a coverage gate, the chain proceeds against incomplete decompositions and ships specs missing whole architectural axes (hooks / event handlers / subagent-driven patterns being the most common omissions). Phase 2.5 is the coverage gate that catches this.
```
**Fidelity:** The incident narrative ("2026-05-12 research run missed hooks…") is ALREADY in `docs/agents/postmortems/2026-05-12-missed-architectural-categories.md` — verified during audit. No postmortem creation needed. SKILL.md restated to surface the lesson (common omitted axes named generically) without the dated incident pointer.

**Postmortem migration:** Confirmed `docs/agents/postmortems/2026-05-12-missed-architectural-categories.md` exists and contains this narrative. No file creation required.

### Hit 4 — line 132
**Category:** Version tag (forward-looking)
**Action:** RESTATE
**Before:**
```
**Iteration cap:** Phase 2.5 runs exactly ONCE. No re-fan-out, no second pass. If the critic missed something the user later spots, that's a v1.8.0+ improvement candidate (Phase 6 CitationAgent equivalent). The bounded loop keeps Phase 2.5 from becoming a coverage-debate hole.
```
**After:**
```
**Iteration cap:** Phase 2.5 runs exactly ONCE. No re-fan-out, no second pass. If the critic missed something the user later spots, capture it as a postmortem candidate. The bounded loop keeps Phase 2.5 from becoming a coverage-debate hole.
```
**Fidelity:** Iteration cap + escape valve preserved. "v1.8.0+ improvement candidate" replaced with "postmortem candidate" — postmortems are the canonical capture mechanism per Hit 4 of File 1 (which restated ADR-0011's rule). "Phase 6 CitationAgent equivalent" parenthetical dropped (forward-planning detail belongs in postmortem analysis, not skill body).

### Hit 5 — line 183
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
This is a heuristic, not a hard gate (consistent with ADR-0013). The user can override with `--quick`, `--balanced`, or `--deep`; the override wins and is recorded with a `(user override)` annotation in the `Tier:` header.
```
**After:**
```
This is a heuristic, not a hard gate. The user can override with `--quick`, `--balanced`, or `--deep`; the override wins and is recorded with a `(user override)` annotation in the `Tier:` header.
```
**Fidelity:** Heuristic + override mechanism preserved. ADR-0013 cite dropped.

### Hit 6 — line 201
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
- **Tier 2 — GitHub repos shipping similar features.** Read the actual code, not just READMEs. Look at the directory structure, the abstractions, the migration history. For NL-framed feature descriptions on Balanced/Deep tiers, run the **semantic-repo-discovery loop** in [`references/semantic-repo-discovery.md`](references/semantic-repo-discovery.md) — fire-rule-gated per [ADR-0017](../../docs/agents/adrs/0017-semantic-repo-discovery-port.md) — to surface candidates that don't compress to keyword queries. Quick skips the loop unconditionally; precise-tech queries fall through to plain `gh search repos` or WebSearch.
```
**After:**
```
- **Tier 2 — GitHub repos shipping similar features.** Read the actual code, not just READMEs. Look at the directory structure, the abstractions, the migration history. For NL-framed feature descriptions on Balanced/Deep tiers, run the **semantic-repo-discovery loop** in [`references/semantic-repo-discovery.md`](references/semantic-repo-discovery.md) — fire-rule-gated to surface candidates that don't compress to keyword queries. Quick skips the loop unconditionally; precise-tech queries fall through to plain `gh search repos` or WebSearch.
```
**Fidelity:** Forward-link to semantic-repo-discovery.md kept (Pattern D forward-pointer). "Per ADR-0017" cite removed; "fire-rule-gated" preserved as the operational fact.

### Hit 7 — line 243
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per [ADR-0018](../../docs/agents/adrs/0018-implement-dormant-artifact-recording-contracts.md) Part B. After Phase 6 composes the report, write it verbatim to:
```
**After:**
```
After Phase 6 composes the report, write it verbatim to:
```
**Fidelity:** Same instruction, prefix dropped. The "Part B" sub-ref doesn't add operational guidance to a Phase 6.5 reader.

### Hit 8 — line 253
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Tier-conditional** per [ADR-0016](../../docs/agents/adrs/0016-chain-wide-depth-tier.md). The tier is in this report's header (`**Tier:**`); fire by tier:
```
**After:**
```
**Tier-conditional.** The tier is in this report's header (`**Tier:**`); fire by tier:
```
**Fidelity:** Rule preserved (tier-conditional, header is source of truth). ADR cite dropped.

### Hit 9 — line 265
**Category:** ADR cite (within parenthetical)
**Action:** RESTATE
**Before:**
```
Research success is not held hostage to archival failure — same shape as the dispatch-record fallback in `parallel-dev` Phase 7.5.
```
**After:**
```
Research success is not held hostage to archival failure — same shape as the dispatch-record fallback in `parallel-dev`.
```
**Fidelity:** Rule preserved. "Phase 7.5" specifier dropped — not because it's a citation but because it requires future-you to know parallel-dev's phase map; "in parallel-dev" is sufficient.

### Hit 10 — line 271
**Category:** Cross-skill inline link (not cruft — verifying Pattern F)
**Action:** KEEP
**Before:**
```
See [`using-habeebs-skill` § "HANDOFF lines — navigation, not state transfer"](../using-habeebs-skill/SKILL.md) for the full-doc-read contract that governs every HANDOFF in the chain.
```
**After:** (unchanged)
**Fidelity:** This is a forward inline `[link](sibling.md)` cross-skill reference — Pattern F (lightweight inline handle). KEEP.

---

**File 2 total: 9 hits requiring action (Hit 10 is KEEP). REMOVE 0, RESTATE 8, MOVE 1, KEEP 1.**

---

## File 3: skills/parallel-dev/SKILL.md (9 hits)

### Hit 1 — line 35
**Category:** ADR cite (multi-paragraph "Task class" section embeds canonical sources URLs as inline citations)
**Action:** RESTATE
**Before:**
```
**Read-task dispatch — Anthropic-validated.** Subagents fetch information, extract patterns, audit, or otherwise *read* shared substrate and return structured records the parent aggregates. No subagent writes to the repo. Anthropic's "Built a multi-agent research system" reports ~90% lift on research-shaped tasks at ~15× the token cost of a single-agent equivalent — the cost is real, the gain is real, the merge-conflict surface is zero because nothing is written. Examples: `prior-art-research` Phase 4-5 source-fetchers, `pattern-extractor` runs, audit-style dispatches.

**Write-task dispatch — Cognition-restricted.** Subagents commit artifacts (code, ADRs, slices, docs) to the repo. Cognition's "Don't build multi-agents" essay names this as the canonical failure mode: "actions carry implicit decisions, and conflicting decisions carry bad results." Parallel writers without isolation diverge irrecoverably. This skill makes the failure mode survivable by enforcing three gates simultaneously — none of which are optional for write tasks:
```
**After:**
```
**Read-task dispatch.** Subagents fetch information, extract patterns, audit, or otherwise *read* shared substrate and return structured records the parent aggregates. No subagent writes to the repo. Research-shaped tasks see ~90% lift at ~15× the token cost of a single-agent equivalent — the cost is real, the gain is real, the merge-conflict surface is zero because nothing is written. Examples: `prior-art-research` Phase 4-5 source-fetchers, `pattern-extractor` runs, audit-style dispatches.

**Write-task dispatch.** Subagents commit artifacts (code, ADRs, slices, docs) to the repo. Parallel writers without isolation diverge irrecoverably: actions carry implicit decisions, and conflicting decisions carry bad results. This skill makes the failure mode survivable by enforcing three gates simultaneously — none of which are optional for write tasks:
```
**Fidelity:** Quantitative claim preserved (~90% lift, ~15× cost). Failure-mode rule preserved (parallel writers diverge irrecoverably). Source-attribution tags dropped ("Anthropic-validated", "Cognition-restricted", "Anthropic's research system", "Cognition's essay"). The Cognition essay quote is paraphrased into the rule directly. **The "Sources for this section" footer at line 45-48 is the right place for these citations** — keep that footer (Pattern D forward-only reference block).

### Hit 2 — line 39
**Category:** Within the same Task class section, same restate covers it
**Action:** RESTATE (covered by Hit 1)

### Hit 3 — line 41-43
**Category:** ADR-style inline cross-references to Cognition essay
**Action:** RESTATE (covered by Hit 1)

### Hit 4 — line 45-48 (Sources block)
**Category:** Backward citation footer — but research-archive style
**Action:** KEEP (with edit)
**Before:**
```
**Sources for this section:**
- [Anthropic — Built a multi-agent research system](https://www.anthropic.com/research/built-a-multi-agent-research-system) — read-task efficacy + 15× token cost
- [Cognition — Don't build multi-agents](https://cognition.ai/blog/dont-build-multi-agents) — write-task anti-pattern
- Audit memo: `docs/agents/research/v1.19.0-workflow-audit-research.md` § "parallel-dev + using-worktrees positioning"
```
**After:** (unchanged, but verify placement)
**Fidelity:** This IS a backward-citation footer block, which Pattern D research-archive convention discourages — BUT this is a load-bearing two-source attribution that the in-line restate explicitly references ("research-shaped tasks see ~90% lift"). Per research recommendation OQ-2 grill outcome, surfacing the empirical sources for the **load-bearing quantitative claim** is acceptable; the rule is "no backward citations to internal ADRs in body prose" — external research-paper citations supporting a quantitative claim are permissible. KEEP this footer.

### Hit 5 — line 94
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
  context_preamble: <REQUIRED — full content of docs/agents/SYSTEM_CONTEXT.md
                    per ADR-0004 Part 3; injected into the subagent's prompt
                    so subagents inherit the parent's environment binding
                    rather than re-running Phase 0 reconnaissance>
```
**After:**
```
  context_preamble: <REQUIRED — full content of docs/agents/SYSTEM_CONTEXT.md
                    injected into the subagent's prompt so subagents inherit
                    the parent's environment binding rather than re-running
                    Phase 0 reconnaissance>
```
**Fidelity:** Rule (context_preamble is required, contains full SYSTEM_CONTEXT.md, prevents Phase 0 re-run) preserved verbatim. "Per ADR-0004 Part 3" cite dropped.

### Hit 6 — line 103
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Context preamble is mandatory** (ADR-0004 Part 3). Without it, subagents drift from the parent's environment binding, re-run Phase 0 reconnaissance, and burn tokens for no reason. The dispatcher reads `docs/agents/SYSTEM_CONTEXT.md` once and injects the content into every subagent's input.
```
**After:**
```
**Context preamble is mandatory.** Without it, subagents drift from the parent's environment binding, re-run Phase 0 reconnaissance, and burn tokens for no reason. The dispatcher reads `docs/agents/SYSTEM_CONTEXT.md` once and injects the content into every subagent's input.
```
**Fidelity:** Identical rule. Parenthetical ADR cite dropped.

### Hit 7 — line 157
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
`docs/agents/SYSTEM_CONTEXT.md` is **read-only for subagents**. The parent agent's `prior-art-research` Phase 0 is the single writer. Dispatch subagents only AFTER Phase 0 has settled (i.e., the parent has completed research-phase recon). This prevents read-during-write races and keeps the environment-binding cache consistent across the parallel batch.
```
**After:** (unchanged — no ADR cite found on inspection. Verifying via grep…)
**Fidelity:** Clean line; no hit. Removing from count.

### Hit 8 — line 188
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per [ADR-0004](../../docs/agents/adrs/0004-parallel-subagent-dispatch-contract.md) Part 2 — implemented by [ADR-0018](../../docs/agents/adrs/0018-implement-dormant-artifact-recording-contracts.md) Part A. After Phase 7 verifies, write **one JSON file** at:
```
**After:**
```
After Phase 7 verifies, write **one JSON file** at:
```
**Fidelity:** Operational instruction preserved (write one JSON file at the path below). Both ADR cites dropped; the dispatch-record-template.md the next line references is the operational source of truth.

### Hit 9 — line 202
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Always-on.** No tier conditionality. ADR-0004 Part 2 mandates every dispatch produces a record; this phase honors that mandate. (Tier governs whether a chain *uses* parallel-dev at all — see ADR-0016 — but once a dispatch happens, recording is unconditional.)
```
**After:**
```
**Always-on.** No tier conditionality. Every dispatch produces a record. (Tier governs whether a chain *uses* parallel-dev at all, but once a dispatch happens, recording is unconditional.)
```
**Fidelity:** Always-on rule + tier-orthogonality rule both preserved. Both ADR cites dropped.

### Hit 10 — line 210
**Category:** Forward cross-link (Pattern D)
**Action:** KEEP
**Before:**
```
This matches the graceful-degradation pattern in [`docs/agents/references/system-context-staleness-check.md`](../../docs/agents/references/system-context-staleness-check.md) § Case A.
```
**After:** (unchanged)
**Fidelity:** Forward link to canonical reference doc — Pattern D allowed.

### Hit 11 — line 212
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Single-writer invariant.** The dispatcher is the sole writer of dispatch records. No downstream skill reads dispatch records during chain execution; the directory is an audit-only log per ADR-0004 Part 2. Forensic readers (`socratic-grill` re-grilling a past failed slice, future calibration scripts) grep across the directory after the fact.
```
**After:**
```
**Single-writer invariant.** The dispatcher is the sole writer of dispatch records. No downstream skill reads dispatch records during chain execution; the directory is an audit-only log. Forensic readers (`socratic-grill` re-grilling a past failed slice, future calibration scripts) grep across the directory after the fact.
```
**Fidelity:** Single-writer + audit-only rules preserved. ADR cite dropped.

### Hit 12 — line 216
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Every subagent dispatched via `parallel-dev` returns exactly one of four statuses (snapshot locked by ADR-0004). The full input + return JSON schemas live in `references/dispatch-record-template.md`; semantics below.
```
**After:**
```
Every subagent dispatched via `parallel-dev` returns exactly one of four statuses. The full input + return JSON schemas live in `references/dispatch-record-template.md`; semantics below.
```
**Fidelity:** Rule preserved. "(snapshot locked by ADR-0004)" parenthetical dropped — operationally the contract IS locked; saying so makes that clear without needing the ADR number.

### Hit 13 — line 257
**Category:** Version tag (forward-looking)
**Action:** REMOVE
**Before:**
```
**Consumer:** `systematic-debugging` Phase 3.5 (planned for v1.8.0+) — when ≥3 hypotheses are on the table, fan out probes via this pattern instead of probing sequentially.
```
**After:**
```
**Consumer:** `systematic-debugging` Phase 3.5 — when ≥3 hypotheses are on the table, fan out probes via this pattern instead of probing sequentially.
```
**Fidelity:** Forward-looking version planning is exactly the cruft category the research targets. The rule (Phase 3.5 dispatches via this pattern when ≥3 hypotheses) is preserved; the "planned for v1.8.0+" speculation dropped. Note: this version planning is now also a stale fact (the planning was v1.8.0+, current version is v1.20.0 — the consumer either exists or it doesn't).

---

**File 3 total: 8 hits requiring action, 1 KEEP (sources footer for empirical claims), 1 KEEP (forward link). REMOVE 1, RESTATE 7, MOVE 0, KEEP 2.**

---

## File 4: skills/tdd-loop/SKILL.md (8 hits)

### Hit 1 — line 32 + 37
**Category:** Version tag (one ADR cite paired)
**Action:** REMOVE (the parenthetical)
**Before:**
```
[Phase 0: decide worktree] → [Phase 0.5: plan inspection — pgroup auto-dispatch + idempotent resume] → RED → GREEN → REFACTOR → 2-stage REVIEW → COMMIT → next slice

...

Phase 0.5 (added v1.7.0) reads the active plan and, when a pgroup of size ≥2 is ready, hands off to `parallel-dev` for concurrent dispatch — each subagent runs its own Phase 1-6 cycle in its own worktree. Single-slice flows pass through Phase 0.5 unchanged.
```
**After:**
```
[Phase 0: decide worktree] → [Phase 0.5: plan inspection — pgroup auto-dispatch + idempotent resume] → RED → GREEN → REFACTOR → 2-stage REVIEW → COMMIT → next slice

...

Phase 0.5 reads the active plan and, when a pgroup of size ≥2 is ready, hands off to `parallel-dev` for concurrent dispatch — each subagent runs its own Phase 1-6 cycle in its own worktree. Single-slice flows pass through Phase 0.5 unchanged.
```
**Fidelity:** Phase description fully preserved. "(added v1.7.0)" archaeology dropped.

### Hit 2 — line 76
**Category:** Version tag + ADR cite combined
**Action:** REMOVE (the parenthetical)
**Before:**
```
(Introduced in v1.7.0 per ADR-0004 and plan 0004. Runs only when an active plan exists at `docs/agents/plans/<slug>.md`. Skipped when there's no active plan or the plan has been flagged Done.)
```
**After:**
```
(Runs only when an active plan exists at `docs/agents/plans/<slug>.md`. Skipped when there's no active plan or the plan has been flagged Done.)
```
**Fidelity:** Operational guards preserved (runs only with active plan; skips if no plan or plan Done). Origin metadata dropped.

### Hit 3 — line 79
**Category:** Forward cross-link (Pattern D)
**Action:** KEEP
**Before:**
```
A **Quick**-tier chain (see the `**Tier:**` field on the spec — [`docs/agents/references/tier-scale.md`](../../docs/agents/references/tier-scale.md)) deliberately skips `write-plan`...
```
**After:** (unchanged)
**Fidelity:** Forward link to canonical tier-scale reference — Pattern D allowed.

### Hit 4 — line 98
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
This is the pause/resume API: git is the durability layer (ADR-0004 Part 4). Killing the chain mid-pgroup and re-running `tdd-loop` will skip completed slices and re-dispatch only pending ones. **No checkpoint file is consulted** — only git refs and dispatch records (audit log).
```
**After:**
```
This is the pause/resume API: git is the durability layer. Killing the chain mid-pgroup and re-running `tdd-loop` will skip completed slices and re-dispatch only pending ones. **No checkpoint file is consulted** — only git refs and dispatch records (audit log).
```
**Fidelity:** Pause/resume mechanism preserved. ADR cite dropped.

### Hit 5 — line 106
**Category:** Cross-skill inline link (Pattern F)
**Action:** KEEP
**Before:**
```
**Step 4 — Status aggregation per the 4-status contract** (canonical semantics in `skills/parallel-dev/SKILL.md` § Return contract):
```
**After:** (unchanged)
**Fidelity:** Forward cross-skill reference via path — Pattern F lightweight handle.

### Hit 6 — line 112
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
| `BLOCKED` | Halt the pgroup; surface the **structured BLOCKED message** (`{type, subagent, slice_id, reason, suggested_action}` per ADR-0004 Part 1) to the user; do NOT auto-re-dispatch |
```
**After:**
```
| `BLOCKED` | Halt the pgroup; surface the **structured BLOCKED message** (`{type, subagent, slice_id, reason, suggested_action}`) to the user; do NOT auto-re-dispatch |
```
**Fidelity:** Schema preserved verbatim. "Per ADR-0004 Part 1" cite dropped — the schema in the parenthetical IS the contract.

### Hit 7 — line 125
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
- It does not write to dispatch records mid-pgroup — only `parallel-dev` writes, after all subagents return (single-writer invariant per ADR-0004 Part 2).
```
**After:**
```
- It does not write to dispatch records mid-pgroup — only `parallel-dev` writes, after all subagents return (single-writer invariant).
```
**Fidelity:** Single-writer rule preserved. ADR cite dropped.

### Hit 8 — line 210
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
**Pass 5c — Anti-slop review (verify-output):** stage the slice's diff (`git add` the relevant files) and invoke [`verify-output`](../verify-output/SKILL.md). The skill scans for the seven slop heuristics (unjustified comments, defensive validation past trusted boundaries, half-finished implementations, dead code, repeated boilerplate, feature creep, backward-compat shims for unshipped code). 4-status return per ADR-0004:
```
**After:**
```
**Pass 5c — Anti-slop review (verify-output):** stage the slice's diff (`git add` the relevant files) and invoke [`verify-output`](../verify-output/SKILL.md). The skill scans for the seven slop heuristics (unjustified comments, defensive validation past trusted boundaries, half-finished implementations, dead code, repeated boilerplate, feature creep, backward-compat shims for unshipped code). 4-status return:
```
**Fidelity:** Rule preserved; cross-skill link KEEP (Pattern F). "Per ADR-0004" trailer dropped.

### Hit 9 — line 217
**Category:** Version tag
**Action:** REMOVE
**Before:**
```
If a `--strict` flag emerges in the future, it would sit between ANNOTATE and GATE — moderate slop becomes blocking only after N concerns are surfaced. Not implemented in v1.9.0.
```
**Wait — this is in verify-output/SKILL.md, not tdd-loop. Reclassifying to File 5.**

### Hit 10 — line 267
**Category:** Forward cross-link with embedded ADR cite
**Action:** RESTATE
**Before:**
```
- [`using-habeebs-skill` § "When sessions grow long — summary-and-flush"](../using-habeebs-skill/SKILL.md) — long tdd-loop runs (20+ slices in one conversation) are the most likely Compress-at-overflow site per [ADR-0012](../../docs/agents/adrs/0012-compress-at-overflow-protocol.md); flush via the 7-section summary template at [`../using-habeebs-skill/references/session-summary-template.md`](../using-habeebs-skill/references/session-summary-template.md)
```
**After:**
```
- [`using-habeebs-skill` § "When sessions grow long — summary-and-flush"](../using-habeebs-skill/SKILL.md) — long tdd-loop runs (20+ slices in one conversation) are the most likely Compress-at-overflow site; flush via the 7-section summary template at [`../using-habeebs-skill/references/session-summary-template.md`](../using-habeebs-skill/references/session-summary-template.md)
```
**Fidelity:** Forward cross-skill link + template link both KEEP (Pattern F). "Per ADR-0012" cite within the See-also entry dropped.

---

**File 4 total: 7 hits requiring action, 2 KEEP (forward links). REMOVE 2, RESTATE 5, MOVE 0, KEEP 2.**

---

## File 5: skills/verify-output/SKILL.md (8 hits)

### Hit 1 — line 9
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Post-generation anti-slop pass. Runs between `tdd-loop` GREEN (tests passing) and the commit. Scans the staged diff against the seven heuristics in [`references/slop-heuristics.md`](references/slop-heuristics.md) and returns a 4-status verdict per [ADR-0004](../../docs/agents/adrs/0004-parallel-subagent-dispatch-contract.md).
```
**After:**
```
Post-generation anti-slop pass. Runs between `tdd-loop` GREEN (tests passing) and the commit. Scans the staged diff against the seven heuristics in [`references/slop-heuristics.md`](references/slop-heuristics.md) and returns a 4-status verdict.
```
**Fidelity:** Operational behavior preserved (returns 4-status verdict). ADR cite dropped — the 4-status table later in the file IS the contract.

### Hit 2 — line 60
**Category:** Version tag
**Action:** REMOVE
**Before:**
```
If a `--strict` flag emerges in the future, it would sit between ANNOTATE and GATE — moderate slop becomes blocking only after N concerns are surfaced. Not implemented in v1.9.0.
```
**After:** (entire paragraph deleted)
**Fidelity:** This is forward-planning prose about a flag that doesn't exist yet. The whole paragraph violates "don't include time-sensitive info" — and it's information about a non-existent flag. The right place to describe a future flag is in an ADR or a roadmap, not a skill body.

### Hit 3 — line 165
**Category:** ADR cite (section heading)
**Action:** RESTATE
**Before:**
```
## 4-status return contract (per ADR-0004)
```
**After:**
```
## 4-status return contract
```
**Fidelity:** Same heading minus parenthetical.

### Hit 4 — line 167
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
This skill's return contract matches the parallel-dev dispatch contract from ADR-0004:
```
**After:**
```
This skill's return contract matches the parallel-dev dispatch contract.
```
**Fidelity:** Contract identity preserved. "From ADR-0004" dropped.

### Hit 5 — line 181
**Category:** ADR cite (in See also footer)
**Action:** RESTATE
**Before:**
```
- [ADR-0008](../../docs/agents/adrs/0008-verify-output-skill-scope.md) — scope, default mode, blocking criteria, separation from deep-modules
- [ADR-0004](../../docs/agents/adrs/0004-parallel-subagent-dispatch-contract.md) — 4-status return contract
```
**After:** (both bullets removed from See also)
**Fidelity:** Per Pattern D (forward-only footer), See also should not list backward-citation ADRs. The body has already had ADR references stripped (Hits 1, 3, 4); listing them in the footer reintroduces backward citation. The ADR index at `docs/agents/adrs/README.md` already lists ADR-0004 and ADR-0008 with "Affects: verify-output, parallel-dev" — that's the canonical back-reference.

### Hit 6 — line 183
**Category:** ADR cite (in See also footer with embedded prose)
**Action:** RESTATE
**Before:**
```
- [ADR-0011](../../docs/agents/adrs/0011-error-analysis-cadence.md) — when verify-output keeps missing a failure class, [`docs/agents/postmortems/`](../../docs/agents/postmortems/) is the canonical place to document the new category and propose a rule for the next release. Static pre-commit check (this skill) and post-incident error analysis (postmortems) are complementary loops, not peers.
```
**After:**
```
- [`docs/agents/postmortems/`](../../docs/agents/postmortems/) — when verify-output keeps missing a failure class, postmortems are the canonical place to document the new category and propose a rule for the next release. Static pre-commit check (this skill) and post-incident error analysis (postmortems) are complementary loops, not peers.
```
**Fidelity:** Rule preserved (postmortems are canonical, two-loop relationship). Link target swapped from ADR-0011 to the postmortems directory itself — Pattern D forward-pointer to bundled-assets.

### Hit 7 — line 189
**Category:** Origins section with embedded cross-references
**Action:** KEEP (this is an Origins block — peer convention surveys showed Origins blocks are mixed; obra/superpowers doesn't use them, Anthropic's skill-creator doesn't either, but they're not strictly backward-citing — they're acknowledgments)
**Before:**
```
## Origins

- Inspired by oh-my-claudecode's [`ai-slop-cleaner`](https://github.com/Yeachan-Heo/oh-my-claudecode) and `ultraqa` — post-generation anti-slop pass framing; "agent stopped mid-thought" pattern recognition
- Lifted from this repo's `CLAUDE.md` — H1-H4 verbatim (user-authored canon, same source of truth)
- `agent-factors-check` applied to verify-output's own design before commit (this skill is itself an LLM workflow with a tool-call schema, state, and a pause/resume case via `NEEDS_CONTEXT`)
```
**After:** (unchanged, but flag for grill — see Open Question 4 below)
**Fidelity:** Origins sections are NOT in the research-validated convention (zero peer SKILL.md files had them). Recommend deletion in v1.21.0 OR migration to a separate `docs/agents/skill-origins.md` registry. **Grill question:** should Origins sections survive v1.21.0 cleanup? See Open Questions section at end.

### Hit 8 — line 9 (counted under Hit 1)
*(already covered)*

---

**File 5 total: 7 hits. REMOVE 2 (Hit 2 forward-flag paragraph + Hit 5 ADR-only footer bullets), RESTATE 4, MOVE 0, KEEP 1 (Origins — flagged for grill).**

---

## File 6: skills/using-worktrees/SKILL.md (1 hit)

### Hit 1 — line 403
**Category:** Version tag
**Action:** REMOVE
**Before:**
```
For cross-session parallel work (the v1.18.0 conflict-detection scenario), this constraint is doing useful work: it forces session-per-branch isolation. Don't try to work around it.
```
**After:**
```
For cross-session parallel work, this constraint is doing useful work: it forces session-per-branch isolation. Don't try to work around it.
```
**Fidelity:** Rule preserved (cross-session work benefits from one-branch-one-worktree). Version archaeology dropped.

### Note: line 405 — Sources block
**Action:** KEEP (with edit)
**Before:**
```
**Source:** [`git-worktree(1)`](https://git-scm.com/docs/git-worktree) — the BUGS and CONFIGURATION FILES sections cover all three. Audit memo: `docs/agents/research/v1.19.0-workflow-audit-research.md` § "using-worktrees".
```
**After:**
```
**Source:** [`git-worktree(1)`](https://git-scm.com/docs/git-worktree) — the BUGS and CONFIGURATION FILES sections cover all three.
```
**Fidelity:** External authoritative source (git-worktree man page) KEPT — this is Pattern D forward-link to bundled-asset-equivalent (external canonical reference). Internal audit-memo backward citation REMOVED — it's exactly the backward-citation pattern Pattern D forbids.

---

**File 6 total: 1 cruft hit + 1 sub-edit on Source block. REMOVE 1, RESTATE 1 (Sources line), MOVE 0, KEEP 0.**

---

## File 7: skills/release/SKILL.md (6 hits)

### Hit 1 — line 80
**Category:** Version tag (section header)
**Action:** REMOVE
**Before:**
```
### Phase 3.25 — Changeset aggregation + path audit (v1.20.0+)
```
**After:**
```
### Phase 3.25 — Changeset aggregation + path audit
```
**Fidelity:** Phase name preserved. Version tag dropped — Phase 3.25 either exists or it doesn't; it does.

### Hit 2 — line 82
**Category:** Version tag (prose)
**Action:** REMOVE
**Before:**
```
**Post-v1.20.0:** the feature branch never edits `plugin.json` / `marketplace.json` / `CHANGELOG.md` directly. Instead, each release-worthy PR drops a `.changeset/<slug>.md` carrying `bump:` + `why:`. This phase is the FIRST step of release-PR creation and runs BEFORE Phase 4 (version bump) — in fact it does the bump.
```
**After:**
```
The feature branch never edits `plugin.json` / `marketplace.json` / `CHANGELOG.md` directly. Instead, each release-worthy PR drops a `.changeset/<slug>.md` carrying `bump:` + `why:`. This phase is the FIRST step of release-PR creation and runs BEFORE Phase 4 (version bump) — in fact it does the bump.
```
**Fidelity:** Whole rule preserved. "Post-v1.20.0" temporal qualifier dropped — the current state IS the post-v1.20.0 state.

### Hit 3 — line 110
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per ADR `adr-late-binding-and-changesets` § Decision: feature branches NEVER directly edit the three aggregated files. Two simultaneous release PRs targeting the same version slot will collide at git merge time (loud, not silent — operator closes the second PR + re-aggregates against post-first-merge state).
```
**After:**
```
Feature branches NEVER directly edit the three aggregated files. Two simultaneous release PRs targeting the same version slot will collide at git merge time (loud, not silent — operator closes the second PR + re-aggregates against post-first-merge state).
```
**Fidelity:** Rule + failure-mode-by-design both preserved. ADR cite dropped.

### Hit 4 — line 114
**Category:** Version tag (section header)
**Action:** REMOVE
**Before:**
```
### Phase 3.5 — ADR ID assignment (late-binding)

**Post-v1.20.0:** any `docs/agents/adrs/adr-*.md` files (unnumbered ADRs written by `decision-record` since the last release) must be assigned sequential integers + renamed + added to the index before tagging.
```
**After:**
```
### Phase 3.5 — ADR ID assignment (late-binding)

Any `docs/agents/adrs/adr-*.md` files (unnumbered ADRs written by `decision-record` since the last release) must be assigned sequential integers + renamed + added to the index before tagging.
```
**Fidelity:** Phase name + operational rule preserved. "Post-v1.20.0:" prefix dropped.

### Hit 5 — line 132
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
Per ADR `adr-late-binding-and-changesets` § Decision: `release` is the SOLE writer of `NNNN-<slug>.md` filenames. `decision-record` is the SOLE writer of `adr-<slug>.md`. The separation is enforced by `tests/dogfood/21-late-binding-adr/check-late-binding.sh`.
```
**After:**
```
`release` is the SOLE writer of `NNNN-<slug>.md` filenames. `decision-record` is the SOLE writer of `adr-<slug>.md`. The separation is enforced by `tests/dogfood/21-late-binding-adr/check-late-binding.sh`.
```
**Fidelity:** Both single-writer rules preserved verbatim. Dogfood-test enforcement reference preserved (Pattern F lightweight path handle, points to the actual enforcement mechanism). ADR cite dropped.

### Hit 6 — line 187
**Category:** Version tag (section heading) + ADR cite combined
**Action:** RESTATE
**Before:**
```
## Description-policy audit (v1.19.0+)

For any release that adds a new SKILL.md or modifies an existing one, the doc-sync audit MUST run `bash tests/dogfood/11-description-budget/check-description-budget.sh` and confirm exit 0 before tagging. This enforces ADR-0007 (amended 2026-05-24) § A-F:

- § A length budget (≤1,024 hard cap, ≤300 avg target)
- § B description anatomy (`[Capability ≤8 words]. [Imperative directive] when [literal user trigger 1], [phrase 2], or [phrase 3]. [Tight anti-trigger].`)
- § C auto-invocation scope (chain-internal skills carry `disable-model-invocation: true`; check `bash tests/dogfood/11-description-budget/check-disabled-list.sh`)
- § E three-keystone anti-trigger thickness (`prior-art-research`, `systematic-debugging`, `deep-modules`)
- OQ-4 block-scalar regression guard (no `|` or `>` on the `description:` line)
```
**After:**
```
## Description-policy audit

For any release that adds a new SKILL.md or modifies an existing one, the doc-sync audit MUST run `bash tests/dogfood/11-description-budget/check-description-budget.sh` and confirm exit 0 before tagging. The audit enforces:

- Length budget: ≤1,024 hard cap, ≤300 avg target
- Description anatomy: `[Capability ≤8 words]. [Imperative directive] when [literal user trigger 1], [phrase 2], or [phrase 3]. [Tight anti-trigger].`
- Auto-invocation scope: chain-internal skills carry `disable-model-invocation: true`; check `bash tests/dogfood/11-description-budget/check-disabled-list.sh`
- Three-keystone anti-trigger thickness (`prior-art-research`, `systematic-debugging`, `deep-modules`)
- Block-scalar regression guard: no `|` or `>` on the `description:` line
```
**Fidelity:** ALL 5 enforcement rules preserved verbatim. Section title cleaned. "ADR-0007 (amended 2026-05-24) § A-F" reference dropped — the rules ARE the rules; the ADR citation doesn't change how the audit runs. "OQ-4" reference (was a grill-question ID from spec phase) also dropped — that's exactly the kind of provenance that doesn't survive cleanup.

---

**File 7 total: 6 hits. REMOVE 4 (Hits 1, 2, 4, and the version tag in Hit 6), RESTATE 2 (Hits 3, 5), MOVE 0, KEEP 0.**

---

## File 8: skills/write-plan/SKILL.md (2 hits)

### Hit 1 — line 41
**Category:** Forward cross-link (Pattern D)
**Action:** KEEP
**Before:**
```
- **Tier** — the `**Tier:**` field from the ADR / spec header (Quick / Balanced / Deep — see [`docs/agents/references/tier-scale.md`](../../docs/agents/references/tier-scale.md)). Echo it into the plan header's `Tier` row.
```
**After:** (unchanged)
**Fidelity:** Forward link to canonical tier-scale reference — Pattern D allowed.

### Hit 2 — line 163-180
**Category:** Version tag (multiple "v1.7.0+" tags in the always-emit block)
**Action:** RESTATE
**Before:**
```
**Always emit (v1.7.0+):** if the plan has any pgroup of size ≥ 2, also emit the pgroup-dispatch-ready handoff so the downstream `tdd-loop` Phase 0.5 knows to auto-dispatch (rather than fall through to single-slice sequential):

[code block]

Both handoff lines are read by `tdd-loop` Phase 0.5 (ADR-0004). The `pgroup-dispatch-ready` line is the *machine-readable* dispatch trigger; the `parallel dispatch ready` line is the *human-readable* heads-up.
```
**After:**
```
**Always emit:** if the plan has any pgroup of size ≥ 2, also emit the pgroup-dispatch-ready handoff so the downstream `tdd-loop` Phase 0.5 knows to auto-dispatch (rather than fall through to single-slice sequential):

[code block]

Both handoff lines are read by `tdd-loop` Phase 0.5. The `pgroup-dispatch-ready` line is the *machine-readable* dispatch trigger; the `parallel dispatch ready` line is the *human-readable* heads-up.
```
**Fidelity:** Always-emit rule preserved. "(v1.7.0+)" version tag + "ADR-0004" cite both dropped.

---

**File 8 total: 1 cruft hit (Hit 2). REMOVE 0, RESTATE 1, MOVE 0, KEEP 1 (forward link).**

---

## File 9: skills/setup-habeebs-skill/SKILL.md (4 hits)

### Hit 1 — line 142
**Category:** ADR cite (multiple in one sentence)
**Action:** RESTATE
**Before:**
```
**Goal:** populate `docs/agents/SYSTEM_CONTEXT.md` so downstream chain skills don't halt on first invocation. ADR-0005 mandates this chain: setup is the bootstrap entry point that *invokes* Phase 0; Phase 0 is the sole *writer* (ADR-0001 single-writer invariant preserved by construction).
```
**After:**
```
**Goal:** populate `docs/agents/SYSTEM_CONTEXT.md` so downstream chain skills don't halt on first invocation. Setup is the bootstrap entry point that *invokes* Phase 0; Phase 0 is the sole writer (single-writer invariant preserved by construction).
```
**Fidelity:** Two rules preserved (setup invokes Phase 0; Phase 0 is sole writer). Two ADR cites dropped.

### Hit 2 — line 159
**Category:** Forward cross-link with embedded ADR-style positioning
**Action:** RESTATE
**Before:**
```
**Single-writer invariant** — `setup-habeebs-skill` MUST NOT write `SYSTEM_CONTEXT.md` directly. It invokes Phase 0. The reconnaissance logic lives in `prior-art-research/SKILL.md` § Phase 0 and stays the single source of truth for environment-binding writes. The canonical staleness/freshness check that Phase 0 enforces is documented at [`docs/agents/references/system-context-staleness-check.md`](../../docs/agents/references/system-context-staleness-check.md).
```
**After:** (unchanged — no inline ADR/version/date to remove; the forward links and rule statement are all Pattern-F/Pattern-D allowed)
**Fidelity:** No cruft. Actually this isn't a hit.

### Hit 3 — line 184
**Category:** ADR cite (in user-facing confirm block)
**Action:** RESTATE
**Before:**
```
  - socratic-grill, write-plan, parallel-dev (read SYSTEM_CONTEXT.md as load-bearing per ADR-0001)
```
**After:**
```
  - socratic-grill, write-plan, parallel-dev (read SYSTEM_CONTEXT.md as load-bearing)
```
**Fidelity:** Rule preserved. "Per ADR-0001" dropped.

### Hit 4 — original count of 2 hits was conservative. On full pass, found:
*(Hits 1 + 3 are the substantive hits; original count of 2 in summary table was correct.)*

---

**File 9 total: 2 hits. REMOVE 0, RESTATE 2, MOVE 0, KEEP 0.**

---

## File 10: skills/devex-review/SKILL.md (1 hit)

### Hit 1 — line 136
**Category:** ADR cite (parenthetical)
**Action:** RESTATE
**Before:**
```
This skill produces one of four statuses at the end of every run (consistent with ADR-0004):
```
**After:**
```
This skill produces one of four statuses at the end of every run:
```
**Fidelity:** Operational rule (produces one of four statuses) preserved. ADR cite dropped — the table immediately below IS the contract.

---

**File 10 total: 1 hit. REMOVE 0, RESTATE 1, MOVE 0, KEEP 0.**

---

## File 11: skills/deep-modules/SKILL.md (2 hits)

### Hit 1 — line 28
**Category:** ADR cite (as a positioning anchor — "ADR-NNNN" used as placeholder)
**Action:** RESTATE
**Before:**
```
- Re-litigating decisions captured in ADRs — if a deepening contradicts an ADR, surface it but mark clearly ("contradicts ADR-NNNN — only worth reopening because…")
```
**After:**
```
- Re-litigating decisions captured in ADRs — if a deepening contradicts an ADR, surface it but mark clearly ("contradicts <ADR title> — only worth reopening because…")
```
**Fidelity:** Anti-pattern rule preserved verbatim. "ADR-NNNN" placeholder (which is a stylized inline citation pattern, not a real number) replaced with "<ADR title>" — same shape, no implied taxonomy of ADR numbers in body.

### Hit 2 — line 37
**Category:** ADR cite
**Action:** RESTATE
**Before:**
```
> **SETUP REQUIRED:** `docs/agents/GLOSSARY.md` missing. Run `/setup` to populate the domain glossary stub. (Per ADR-0005, GLOSSARY.md is the domain-vocabulary half of habeebs-skill's two-file context layout; `deep-modules` cannot propose names without it.)
```
**After:**
```
> **SETUP REQUIRED:** `docs/agents/GLOSSARY.md` missing. Run `/setup` to populate the domain glossary stub. GLOSSARY.md is the domain-vocabulary half of the two-file context layout; `deep-modules` cannot propose names without it.
```
**Fidelity:** Setup-required halt message + reasoning preserved. ADR cite dropped.

---

**File 11 total: 2 hits. REMOVE 0, RESTATE 2, MOVE 0, KEEP 0.**

---

## Summary statistics (post-audit)

| File | Total cruft hits | Forward-link KEEPs |
|---|---|---|
| using-habeebs-skill | 14 | 0 |
| prior-art-research | 9 | 1 |
| parallel-dev | 8 | 2 |
| tdd-loop | 7 | 2 |
| verify-output | 7 | 1 (Origins — grill) |
| using-worktrees | 1 | 0 |
| release | 6 | 0 |
| write-plan | 1 | 1 |
| setup-habeebs-skill | 2 | 0 |
| devex-review | 1 | 0 |
| deep-modules | 2 | 0 |
| **TOTAL** | **58 cruft hits** | **7 KEEPs (forward links)** |

**Of the 58 cruft hits:**
- **REMOVE:** 11 (forward-flag paragraphs, version tags in headers, redundant cites)
- **RESTATE:** 46 (preserve rule, drop citation/tag/date)
- **MOVE-TO-POSTMORTEM:** 1 (the 2026-05-12 incident — confirmed already exists at `docs/agents/postmortems/2026-05-12-missed-architectural-categories.md`; no new file needed)

Note: original spec estimate was 69 hits across 12 files. Actual count: **58 hits across 11 files** (`socratic-grill` was on the original list but not on re-grep). The 11 vs 69 discrepancy is from:
- Original count grep'd raw matches; the audit consolidated clustered cites within one sentence
- 1 file dropped (socratic-grill)
- Several "hits" turned out to be Pattern-D/Pattern-F allowed forward links

---

## Open questions for grill (before applying cleanup)

### OQ-A1 — Origins sections (verify-output line 187, possibly others)

verify-output has a `## Origins` section attributing the design to oh-my-claudecode's ai-slop-cleaner + ultraqa + CLAUDE.md. Zero peer SKILL.md files in the research had Origins sections. Three options:

- (a) Delete in v1.21.0 (matches research convention)
- (b) Move to `docs/agents/skill-origins.md` registry (preserves attribution, removes from body)
- (c) Keep (attribution-as-courtesy)

**Recommend:** (b) — move to a separate origins registry. Pattern D allows forward-pointers from skill body if needed.

### OQ-A2 — parallel-dev "Sources for this section" footer (line 45-48)

parallel-dev has a 3-bullet sources block at the end of its "Task class" section citing Anthropic's research-system paper + Cognition's "Don't build multi-agents" + the v1.19.0 audit memo. The quantitative claim in the body ("~90% lift at ~15× token cost") is from the Anthropic paper. Per the research:

- External research-paper citations supporting a quantitative claim are **arguably defensible**
- The audit-memo backward citation is **clearly cruft** (internal backward citation)

**Recommend:** Keep the two external citations; delete the audit-memo bullet. Restate the section intro accordingly.

### OQ-A3 — using-worktrees "Audit memo" reference (line 405)

Same pattern as OQ-A2 but in using-worktrees — the Source block includes both an external git-worktree man page link AND an internal audit-memo backward citation.

**Recommend:** Keep the external git-worktree link; delete the audit-memo bullet.

### OQ-A4 — Cross-skill links that include ADR-citation embedded in their description

Multiple See-also blocks contain entries like `- [ADR-0011](path) — when verify-output keeps missing...`. Per Pattern D research, See-also footers should be **forward-pointing to bundled assets or sibling skills**, NOT backward-citing ADRs. But these entries carry **useful operational guidance in their descriptions** ("when X happens, look here").

**Recommend:** Convert each from `[ADR-NNNN]` link target to the most-operationally-useful forward link (e.g., `[docs/agents/postmortems/]` for ADR-0011's content), preserving the description prose. Demonstrated in Hit 6 of File 5.

### OQ-A5 — Should the audit treat the `## Description-policy audit (v1.19.0+)` heading + the dated amendment ("amended 2026-05-24") as one combined cruft cluster or separately?

Demonstrated in Hit 6 of File 7 — treated as one cluster, fully restated. Confirm this is the right granularity.

### OQ-A6 — Worst-offender semantic-load probe (from OQ-2 in the spec)

The two 13-hit files (using-habeebs-skill, prior-art-research) had their highest-risk restatements applied:
- **using-habeebs-skill Hit 1** (line 39) — the OpenAI / Cognition / ADR-0004 cluster supporting the "full-doc-read contract." Restatement keeps the rule + drops 3 citations.
- **using-habeebs-skill Hit 8 + 9** (lines 105-111) — the "Standalone by design (ADR-0002)" section heading + closing link.
- **prior-art-research Hit 1** (line 41) — the "per ADR-0005 single-writer invariant" qualifier on Phase 0 writer rule.

**Pressure-test:** does removing the "per ADR-0005" qualifier on the SYSTEM_CONTEXT writer rule weaken enforcement? Answer: no — the rule is stated as a direct fact ("prior-art-research is the canonical writer; other chain skills only read"). The ADR-0005 citation was a provenance tag, not a load-bearing element of the rule.

### OQ-A7 — Restated-rule fidelity spot-check (from OQ-3 in the spec)

5 hardest restatements presented for grill validation:
1. **using-habeebs-skill Hit 1** — Big paragraph; 3 external citations dropped. Fidelity: rule preserved as direct statement.
2. **parallel-dev Hit 1** — Two-paragraph Task class intro; quantitative claim + failure-mode rule preserved; source-attribution tags dropped.
3. **prior-art-research Hit 3 (dated incident)** — Incident narrative replaced with generic axis-omission lesson. Fidelity: surface lesson preserved; incident-specific learning archived in already-existing postmortem.
4. **release Hit 6** — Description-policy audit. 5 enforcement rules preserved verbatim; ADR-0007 + amendment-date + OQ-4 reference dropped.
5. **deep-modules Hit 1** — "ADR-NNNN" stylized placeholder replaced with "<ADR title>" preserving the anti-pattern rule shape.

All five pass fidelity check.

---

## Approval gate (signed off per file by Modie before any edits land)

- [ ] skills/using-habeebs-skill/SKILL.md (14 hits)
- [ ] skills/prior-art-research/SKILL.md (9 hits, 1 postmortem-move)
- [ ] skills/parallel-dev/SKILL.md (8 hits, 2 KEEP forward links)
- [ ] skills/tdd-loop/SKILL.md (7 hits, 2 KEEP forward links)
- [ ] skills/verify-output/SKILL.md (7 hits, 1 KEEP pending grill on Origins)
- [ ] skills/using-worktrees/SKILL.md (1 hit + 1 sub-edit)
- [ ] skills/release/SKILL.md (6 hits)
- [ ] skills/write-plan/SKILL.md (1 hit + 1 KEEP forward link)
- [ ] skills/setup-habeebs-skill/SKILL.md (2 hits)
- [ ] skills/devex-review/SKILL.md (1 hit)
- [ ] skills/deep-modules/SKILL.md (2 hits)

---

## Next steps

1. **Grill the 7 open questions (OQ-A1 through OQ-A7).** Especially OQ-A1 (Origins fate) and OQ-A2/A3 (external research-paper citations as exception).
2. **Per-file approval** — Modie reviews each file's diff before edits land. Recommend lowest-hit files first (devex-review, deep-modules, setup-habeebs-skill, write-plan, using-worktrees) as tracer; worst offenders last.
3. **Postmortem migration** — verified `docs/agents/postmortems/2026-05-12-missed-architectural-categories.md` already exists; no creation needed.
4. **Dogfood scenarios 26/27/28** — regression-prevention greps; can be built in parallel with the per-file cleanups.

HANDOFF: audit approved → invoke per-file cleanup in lowest-to-highest hit order.
HANDOFF: grill OQ-A1 through OQ-A7 first (especially A1, A2, A3) so cleanup mechanics are locked.

---

## 2026-05-26 Amendment (post-grill)

The grill at `v1.21.0-body-cleanup-grill.md` resolved all 7 OQs. Two amendments to the audit:

### OQ-A1 reversal — Origins section gets DELETED, not migrated to registry

The audit recommended (b) move verify-output's `## Origins` section to a `docs/agents/skill-origins.md` registry. Grill overruled this with (a) **delete the Origins section entirely**. Reasoning:

- Zero peer precedent for centralized origins registries (research sampled 27 instruction files)
- A 1-entry registry (oh-my-claudecode → verify-output is the only genuine external attribution) fails the ADR-0009 3-consumer threshold
- The CLAUDE.md attribution + the agent-factors-check self-reference in the current Origins block are redundant
- Attribution preserved in v1.21.0 commit message (`git log --all --grep="oh-my-claudecode"`)
- Optional: HTML-commented `<!-- Inspired by oh-my-claudecode/ai-slop-cleaner + ultraqa -->` line in the SKILL.md body (dogfood scenarios 26-28 exclude HTML-commented regions per Slice 6 acceptance criteria)

**Revisit trigger:** 3+ skills need genuine external attribution → promote to `docs/agents/skill-origins.md` registry then.

### OQ-A7 Probe 2 refinement — prior-art-research dated-incident restatement

Audit's restatement:

> "Without a coverage gate, the chain proceeds against incomplete decompositions and ships specs missing whole architectural axes (hooks / event handlers / subagent-driven patterns being the most common omissions)."

Grill found "most common omissions" overstates the evidence base (1 well-documented incident + several poorly-documented). Refined final text:

> "Without a coverage gate, the chain proceeds against incomplete decompositions and ships specs missing whole architectural axes (for example: hooks / event handlers / subagent-driven patterns)."

The "for example" framing keeps the categories as illustrations rather than implying empirical frequency.

### OQ-A7 Probe 3 refinement — deep-modules placeholder

Audit's restatement:

> "Re-litigating decisions captured in ADRs — if a deepening contradicts an ADR, surface it but mark clearly ('contradicts <ADR title> — only worth reopening because…')"

Grill found `<ADR title>` is meta-syntax that invites over-thinking. Refined final text:

> "Re-litigating decisions captured in ADRs — if a deepening contradicts a locked ADR, surface it but mark clearly ('contradicts the X ADR — only worth reopening because…')"

The placeholder `X` is small enough to read as "fill in the rule name" without inviting template-mechanics confusion.

### Codified rule from OQ-A2 + OQ-A3 (new — applies to parallel-dev + using-worktrees + any future skill)

**Pattern-D exception for empirical claims:** A `## Sources for this section:` footer (or similar) is permitted when the body contains a load-bearing quantitative claim (a specific number, percentage, or rate) that an operator might need to defend. Each footer entry MUST be a forward link to an external authoritative source (peer-reviewed paper, vendor engineering blog, canonical man page) — never to an internal ADR, postmortem, audit memo, or in-repo document.

This means:
- parallel-dev's two external links (Anthropic research-system paper, Cognition essay) KEEP; the internal audit-memo line DROPS
- using-worktrees's git-worktree(1) link KEEPS; the internal audit-memo line DROPS

### Codified rule from OQ-A4 — See-also ADR-link conversion table

| Original See-also entry | Converted forward target |
|---|---|
| `[ADR-0011]` (error-analysis cadence) | `docs/agents/postmortems/` |
| `[ADR-0004]` (parallel-subagent-dispatch contract) | `skills/parallel-dev/SKILL.md` § Return contract |
| `[ADR-0008]` (verify-output skill scope) | DELETE entry (scope already in skill body) |
| `[ADR-0007]` (description policy) | `tests/dogfood/11-description-budget/` |
| `[ADR-0015]` (hook tag-push allowance) | `hooks/` directory |
| `[ADR-0003]` (hooks scope) | `hooks/` directory |

The ADR index at `docs/agents/adrs/README.md` continues to provide the canonical reverse-lookup via each ADR's `## Affects:` back-reference.

### Pre-Slice-2 verification (new requirement)

Before Slice 2 starts, verify `docs/agents/adrs/README.md` has up-to-date `## Affects:` back-references for ADRs 0001-0021. If stale, the cleanup creates a discoverability gap (DX-2 from the grill). If stale, refresh the README first.
