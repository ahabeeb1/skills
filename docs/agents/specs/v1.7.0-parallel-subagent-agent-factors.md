# Agent-Factors-Check: v1.7.0 Parallel Subagent Processing

**Spec:** [`v1.7.0-parallel-subagent.md`](./v1.7.0-parallel-subagent.md)
**Checked on:** 2026-05-12
**Source:** [humanlayer/12-factor-agents](https://github.com/humanlayer/12-factor-agents)

## Trigger justification

This spec describes changes to the habeebs-skill chain — an LLM/agent workflow where subagent Task calls are on the critical path. The new `parallel-dev` dispatch is a tool-call-like surface (subagent name + structured input + structured return). The 12-factor lens applies. Running the check.

## Scoring

| Factor | Score | Note |
|---|---|---|
| F1 — NL to Tool Calls | ~Partial | 4-status return contract exists but subagent-call **input** schema undefined |
| F2 — Own Your Prompts | ✓ Addressed | Slices #1-#3 + category-completeness-critic = 4 in-repo prompt files; versioned via git |
| F3 — Own Your Context Window | ✗ Missing | Spec is silent on whether `SYSTEM_CONTEXT.md` is injected into each subagent's prompt |
| F4 — Tools = Structured Outputs | ~Partial | 4-status return shape implied but no JSON schema |
| F5 — Unify State | ~Partial | Q2 grilling this — dispatch record location |
| F6 — Launch/Pause/Resume | ✗ Missing | What happens if a subagent dispatch is interrupted mid-flight? Spec silent |
| F7 — Human as Tool | ~Partial | BLOCKED status surfaces to user — but via what shape? Spec implies "free-form text"; F7 prefers structured |
| F8 — Own Control Flow | ✓ Addressed | `tdd-loop` Phase 0.5 explicitly orchestrates pgroup dispatch |
| F9 — Compact Errors | ~Partial | "log concerns inline" is vague |
| F10 — Small Focused Agents | ✓ Addressed | 3 bounded agent prompts + 1 category-critic |
| F11 — Trigger From Anywhere | ~Partial | Only `tdd-loop` and `prior-art-research` invoke `parallel-dev`; `/parallel` exists. New trigger surfaces? |
| F12 — Stateless Reducer | ✓ Addressed | Worktree isolation + independence proof per `parallel-dev` Phase 2 |
| F13 — Pre-fetch Context | ✗ Missing | Same as F3 — context-preamble for subagents |

**Counts:** 4 Addressed / 6 Partial / 3 Missing.

## Questions added to grilling agenda

### Must-grill (high blast radius)

- **AF-F6** — Pause/resume semantics when subagent dispatch is interrupted (user Ctrl-C, process kill, network drop)
- **AF-F7** — Human escalation surface and message shape when subagent returns BLOCKED

### Should-grill (medium blast radius)

- **AF-F1 / F4 (combined)** — Subagent call input schema + 4-status return JSON schema
- **AF-F13** — Pre-fetch `SYSTEM_CONTEXT.md` into each subagent's prompt — yes/no and how

### Nice-to-grill (skipped unless user requests full sweep)

- **AF-F9** — DONE_WITH_CONCERNS: what does "log concerns inline" mean concretely?
- **AF-F11** — New trigger surfaces beyond `tdd-loop` and `prior-art-research`?

## Handoff

```
HANDOFF: grilling agenda updated — 6 new questions added (4 high+medium + 2 light-touch).
Resume socratic-grill with these questions interleaved into the existing Q1-Q8 agenda.
```
